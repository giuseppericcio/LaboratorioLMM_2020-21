abilitazione clock:
gpioa
dac
dma
tim2

-----------------------------------------------------------------------------

tck = N * tc

tc intervallo di tempo tra un campione e l'altro = tempo di update del tim2
N nr campioni

Esempio: 200 campioni

-----------------------------------------------------------------------------

configurare: 
pa4 in mod analogica
channel 3 del DMA

------------------------------------------------------------------------------

impostare:
incremento MINC
mod Circolare
direzione mem2per (bit a 1)
CPAR uguale al DHR del DAC  (DMA1_Channel3->CPAR = &(DAC->DHR12R1))
CMAR uguale all'array di campioni generati (DMA2_Channel3->CMAR = (unsigned int) &(nome_array[indice]))

------------------------------------------------------------------------------

generazione sinusoide continua con 100 campioni con periodo di 100ms sul canale 1.

acquisire dal sensore di temperatura in maniera continua.